扩展激活后，你的模型会立即出现在 Copilot Chat 的模型选择器中。

如果没有看到，可能只是列表较长——向下滚动，查找你在 `yoke.customModels` 中为每个条目设置的 `name`。若未配置任何自定义模型，则会显示内置的 DeepSeek 云端模型，那些确实需要 API Key。

如果某个模型完全没有出现，请打开 `Yoke: 查看日志`——无效的 `customModels` 条目会被跳过，原因会记录在那里。
